sudo: required
language:  python

python:
  - 2.7
  - 3.5
  - 3.6
  - 3.7
  - 3.8
  - 3.9

install:
  - pip install zc.buildout
  - pip install setuptools==36.8.0
  - buildout bootstrap
  - buildout

script:
  - bin/test
